# @package _global_
observation_type: state

control:
  controller: diffik

action_horizon: 8
pred_horizon: 32
obs_horizon: 1

furniture: one_leg
randomness: [med, med_perturb]
demo_source: teleop
early_stopper:
  patience: inf

training:
  batch_size: 256
  steps_per_epoch: -1
  checkpoint_interval: 50
  num_epochs: 2000

rollout:
  every: 10
  max_steps: 850
  num_envs: 256
  loss_threshold: 0.1
  randomness: low

wandb:
  project: ol-state-dr-1
