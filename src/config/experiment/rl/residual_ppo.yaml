# @package _global_
base_policy:
  # wandb_id: ol-state-dr-low-1/6i7hupje
  # wt_type: best_success_rate
  wandb_id: ol-state-dr-med-1/runs/otft0k6k
  wt_type: "_1499.pt"

actor:
  residual_policy:
    pretrained_wts: null

env:
  randomness: med

wandb:
  entity: robust-assembly
  project: residual-ppo-dr-${env.randomness}-1
  mode: online

debug: false
