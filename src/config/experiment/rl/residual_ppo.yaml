# @package _global_
actor:
  residual_policy:
    pretrained_wts: null

base_policy:
  wandb_id: ???
  wt_type: best_success_rate

env:
  randomness: med

wandb:
  entity: robust-assembly
  project: ol-rppo-dr-${env.randomness}-1
  mode: online

debug: false
